# Build outputs
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/

# Android / modules
androidApp/build/
desktopApp/build/
core/build/

# Keystore (never commit)
*.jks
*.keystore
# ...except the public sideload keystore (stable signature for tap-install builds)
!app/sideload.jks

# PDF crop artifacts (original preserved in git history; backup is redundant)
Spiewnik_original.pdf
*.pdf.tmp
crop_preview/
